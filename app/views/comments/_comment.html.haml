%p= comment.comment
%p= comment.user.email

.btn-group
  = link_to "Edit", edit_post_comment_path(comment.post, comment), class: "btn btn-warning"
  = link_to "Delete", [comment.post, comment], method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger"
